{
  "name": "code-point-mapping",
  "version": "0.2.0",
  "license": "MIT",
  "description": "Map between javascript string indices and unicode code point offsets effectively",
  "repository": "github:ConradIrwin/code-point-mapping",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepack": "rm -rf ./dist && tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.3"
  }
}
